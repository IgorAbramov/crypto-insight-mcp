# Only needed for the optional LLM agent demo (agent_demo/demo.py --llm).
# The MCP server, REST API, tests and the offline demo need NO keys at all.
ANTHROPIC_API_KEY=

# Optional overrides
# CIM_CHROMA_DIR=.chroma            # Chroma persist directory
# CIM_KB_DIR=knowledge_base         # knowledge base source directory
# CIM_FORCE_HASH_EMBEDDINGS=1       # skip ONNX model, use offline hash embeddings
# CIM_DEMO_MODEL=claude-sonnet-4-5  # model for the LLM demo mode
